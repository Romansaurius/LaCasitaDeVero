services:
  - type: web
    name: lacasitadevero
    env: php
    buildCommand: composer install --no-dev --optimize-autoloader || true
    startCommand: php -S 0.0.0.0:$PORT -t .
    envVars:
      - key: PHP_VERSION
        value: 8.1
